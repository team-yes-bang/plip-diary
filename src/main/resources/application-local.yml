server:
  port: ${SERVER_PORT:8081}

spring:
  mongodb:
    uri: mongodb://${MONGO_HOST:localhost}:${MONGO_PORT:27017}/${MONGO_DATABASE:plip_diary_read}
  datasource:
    url: jdbc:mysql://localhost:${MYSQL_PORT:3308}/${MYSQL_DATABASE:plip_diary}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    prefer-ip-address: false
    hostname: localhost
  client:
    enabled: false
    service-url:
      defaultZone: http://localhost:8761/eureka/
