spring:
  kafka:
    consumer:
      group-id: diary-service-test
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration

app:
  kafka:
    topics:
      user-registered: user.registered
      diary-video-uploaded: diary.video.uploaded
      diary-video-linked: diary.video.linked
      diary-video-unlinked: diary.video.unlinked
